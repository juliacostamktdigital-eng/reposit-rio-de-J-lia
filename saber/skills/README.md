# Skills SABER

Fonte oficial atual: `canonicas/`.

Esta pasta foi reorganizada em 2026-05-13 para usar como base canonica a arvore `brain_v4_colli/empresa/skills/saber/skills/canonicas`.

O conteudo anterior de `saber/skills` foi preservado em `saber/legacy/skills-pre-canonicas-2026-05-13/`.

## Regras

- Use `canonicas/{skill}/SKILL.md` para importacao no Paperclip.
- Use `canonicas/{skill}/latest.md` para leitura humana da versao vigente.
- Ao alterar uma skill, crie novo `vX.Y.Z.md`, atualize `latest.md`, `SKILL.md`, `context.md`, `META.md` e `CHANGELOG.md`.
- Nao edite versoes `vX.Y.Z.md` antigas.
